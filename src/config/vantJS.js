import Vue from "vue";
import "vant/lib/index.css";
import {
    Button,
    Toast,
    Cell,
    CellGroup,
    Icon,
    Tag,
    Tabs,
    Tab,
    Row,
    Col,
    Checkbox,
    Circle
} from "vant";

Vue.use(Button)
.use(Cell)
.use(CellGroup)
.use(Icon)
.use(Tag)
.use(Tabs)
.use(Tab)
.use(Row)
.use(Col)
.use(Checkbox)
.use(Circle)
.use(Toast);
